{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: app commands are not ACL-gated (verified: attach worked with an empty capability set), but PLUGIN commands are — the webview's notification calls fail silently without the grant below.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
